import { UnitSearchPipe } from './unit-search.pipe';

describe('UnitSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
