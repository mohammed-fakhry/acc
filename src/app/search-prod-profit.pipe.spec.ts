import { SearchProdProfitPipe } from './search-prod-profit.pipe';

describe('SearchProdProfitPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProdProfitPipe();
    expect(pipe).toBeTruthy();
  });
});
