import { StockTransactionD } from './stock-transaction-d';

describe('StockTransactionD', () => {
  it('should create an instance', () => {
    expect(new StockTransactionD()).toBeTruthy();
  });
});
