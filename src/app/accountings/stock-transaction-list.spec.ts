import { StockTransactionList } from './stock-transaction-list';

describe('StockTransactionList', () => {
  it('should create an instance', () => {
    expect(new StockTransactionList()).toBeTruthy();
  });
});
