import { ProductStockSearchPipe } from './product-stock-search.pipe';

describe('ProductStockSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductStockSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
